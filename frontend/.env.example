# API URL - use relative path for Vite proxy in development
VITE_API_URL=/api
# Leave empty when using Vite proxy (requests proxied to backend)
VITE_APP_URL=

# Pusher configuration
VITE_PUSHER_APP_KEY=your-app-key
VITE_PUSHER_APP_CLUSTER=mt1
